import { ComingSoon } from "@/components/portal/ComingSoon";

export const metadata = { title: "ETA e-invoicing — Admin" };

export default function AdminEtaPage() {
  return (
    <ComingSoon
      title="ETA e-invoicing"
      blurb="Issue and submit subscription e-invoices to the Egyptian Tax Authority. This admin tool is being built."
    />
  );
}
